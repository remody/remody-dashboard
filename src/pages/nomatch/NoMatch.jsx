import React from "react";
import ErrorLink from "components/ErrorLink";

const NoMatch = () => {
    return (
        <div>
            <ErrorLink />
        </div>
    );
};

export default NoMatch;
